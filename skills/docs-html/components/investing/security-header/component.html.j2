{# purpose: identity strip for the security under analysis: ticker, name, key market data #}
{# facts: list of (label, value) — price, market cap, sector, float, whatever the document needs #}
{% macro security_header(ticker="", name="", exchange="", asof="", facts=[]) %}
  <header class="sec-header">
    <p class="sec-id">
      <span class="sec-ticker">{{ ticker }}</span>
      <span class="sec-name">{{ name }}</span>
      {% if exchange %}<span class="sec-exchange">{{ exchange }}</span>{% endif %}
    </p>
    {% if facts %}
    <dl class="sec-facts">
      {% for label, value in facts %}
      <div><dt>{{ label }}</dt><dd>{{ value }}</dd></div>
      {% endfor %}
    </dl>
    {% endif %}
    {% if asof %}<p class="sec-asof">Market data as of {{ asof }}</p>{% endif %}
  </header>
{% endmacro %}
