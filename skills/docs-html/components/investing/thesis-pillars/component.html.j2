{# purpose: the load-bearing arguments of a thesis: claim, evidence, and what would break it #}
{# items: list of (claim, evidence, falsifier) #}
{% macro thesis_pillars(items=[]) %}
  <ol class="pillars">
  {% for claim, evidence, falsifier in items %}
    <li class="pillar">
      <p class="pillar-claim">{{ claim }}</p>
      {% if evidence %}<p class="pillar-evidence">{{ evidence }}</p>{% endif %}
      {% if falsifier %}<p class="pillar-falsifier"><span class="pillar-tag">Breaks if</span> {{ falsifier }}</p>{% endif %}
    </li>
  {% endfor %}
  </ol>
{% endmacro %}
