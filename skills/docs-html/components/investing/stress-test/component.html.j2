{# purpose: portfolio or company impact under named stress scenarios #}
{# rows: list of (scenario, trigger, impact, magnitude, tone, response) — magnitude is a number in percent for the bar #}
{% macro stress_test(rows=[], caption="", note="") %}
  <table class="fin stress">
  {% if caption %}<caption>{{ caption }}</caption>{% endif %}
    <thead><tr><th>Scenario</th><th>Trigger</th><th class="stress-impact-head">Impact</th><th>Response</th></tr></thead>
    <tbody>
    {% for scenario, trigger, impact, magnitude, tone, response in rows %}
      <tr>
        <th scope="row">{{ scenario }}</th>
        <td class="stress-trigger">{{ trigger }}</td>
        <td class="stress-impact">
          <span class="stress-value tone-{{ tone }}">{{ impact }}</span>
          <span class="stress-track"><span class="stress-fill stress-fill-{{ tone }}" data-pct="{{ magnitude }}%"></span></span>
        </td>
        <td class="stress-response">{{ response }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% if note %}<p class="st-source">{{ note }}</p>{% endif %}
{% endmacro %}
