{# purpose: portfolio positions with a weight bar, plus whatever columns the review needs #}
{# rows: list of (name, weight, cells, tone) — weight is a number in percent; tone tints the last cell #}
{% macro holdings_table(headers=[], rows=[], caption="", total_row=none) %}
  <table class="investing-fin holdings">
  {% if caption %}<caption>{{ caption }}</caption>{% endif %}
    <thead><tr><th>Position</th><th class="investing-holdings-weight-head">Weight</th>{% for h in headers %}<th>{{ h }}</th>{% endfor %}</tr></thead>
    <tbody>
    {% for name, weight, cells, tone in rows %}
      <tr>
        <th scope="row">{{ name }}</th>
        <td class="investing-holdings-weight">
          <span class="investing-holdings-track"><span class="investing-holdings-fill" data-pct="{{ weight }}%"></span></span>
          <span class="investing-holdings-pct">{{ weight }}%</span>
        </td>
        {% for v in cells %}<td{% if loop.last and tone %} class="investing-tone-{{ tone }}"{% endif %}>{{ v }}</td>{% endfor %}
      </tr>
    {% endfor %}
    </tbody>
    {% if total_row %}
    <tfoot><tr class="investing-total"><th scope="row">{{ total_row[0] }}</th><td>{{ total_row[1] }}</td>{% for v in total_row[2] %}<td>{{ v }}</td>{% endfor %}</tr></tfoot>
    {% endif %}
  </table>
{% endmacro %}
