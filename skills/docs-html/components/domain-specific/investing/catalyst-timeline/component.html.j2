{# purpose: dated catalysts ahead, with expected direction and likelihood #}
{# items: list of (date, event, direction, likelihood, note) — direction up|down|mixed #}
{% macro catalyst_timeline(items=[]) %}
  <ol class="investing-catalysts">
  {% for date, event, direction, likelihood, note in items %}
    <li class="investing-catalyst investing-catalyst-{{ direction }}">
      <span class="investing-catalyst-date">{{ date }}</span>
      <span class="investing-catalyst-dir" aria-hidden="true">{{ {"up": "▲", "down": "▼"}.get(direction, "◆") }}</span>
      <span class="investing-catalyst-body">
        <span class="investing-catalyst-event">{{ event }}</span>
        {% if likelihood %}<span class="investing-catalyst-likelihood">{{ likelihood }}</span>{% endif %}
        {% if note %}<span class="investing-catalyst-note">{{ note }}</span>{% endif %}
      </span>
    </li>
  {% endfor %}
  </ol>
{% endmacro %}
