{# purpose: customer unit economics: acquisition cost, lifetime value, payback and the ratio #}
{# rows: list of (metric, value, benchmark, note) #}
{% macro unit_economics(rows=[], headline=[], caption="", verdict="", tone="neutral", benchmark_label="Benchmark", note="") %}
  {% if headline %}
  <dl class="investing-unit-headline investing-unit-{{ tone }}">
    {% for label, value in headline %}
    <div><dt>{{ label }}</dt><dd>{{ value }}</dd></div>
    {% endfor %}
  </dl>
  {% endif %}
  <table class="investing-fin investing-uniteco">
  {% if caption %}<caption>{{ caption }}</caption>{% endif %}
    <thead><tr><th>Metric</th><th>Value</th><th>{{ benchmark_label }}</th><th>Reading</th></tr></thead>
    <tbody>
    {% for metric, value, benchmark, reading in rows %}
      <tr>
        <th scope="row">{{ metric }}</th>
        <td class="investing-rm-value">{{ value }}</td>
        <td>{{ benchmark }}</td>
        <td class="investing-rm-note">{{ reading }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
  {% if verdict %}<p class="investing-unit-verdict investing-unit-verdict-{{ tone }}">{{ verdict }}</p>{% endif %}
  {% if note %}<p class="investing-st-source">{{ note }}</p>{% endif %}
{% endmacro %}
