{# purpose: double-entry accounting posting (debit/credit) #}
{# lines: list of (account, debit, credit) — credit lines leave debit "" #}
{% macro journal_entry(id="", date="", memo="", lines=[]) %}
  <div class="business-journal">
    <div class="business-journal-head">
      {% if date %}<span class="business-journal-date">{{ date }}</span>{% endif %}
      {% if id %}<span class="business-journal-id">{{ id }}</span>{% endif %}
    </div>
    <table>
      <thead><tr><th>Account</th><th>Debit</th><th>Credit</th></tr></thead>
      <tbody>
      {% for account, debit, credit in lines %}
        <tr><td{% if not debit %} class="business-credit-account"{% endif %}>{{ account }}</td><td>{{ debit }}</td><td>{{ credit }}</td></tr>
      {% endfor %}
      </tbody>
    </table>
  {% if memo %}
    <p class="business-journal-memo">{{ memo }}</p>
  {% endif %}
  </div>
{% endmacro %}
