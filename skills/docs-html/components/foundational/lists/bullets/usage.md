# bullets

_Authoring guidance for the `bullets` component — when to use it, how, and the rules._

An unordered list. Macro: `bullets(items)` where items is a list of strings.
Use for unordered points; for procedures use `steps`, for pass/fail use `checklist`.
