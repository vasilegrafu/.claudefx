# numbered

_Authoring guidance for the `numbered` component — when to use it, how, and the rules._

An ordered list (plain `<ol>`). Macro: `numbered(items)` — list of strings.
For procedures use `steps`; for pass/fail state use `checklist`.
