# Layout — layout

Arrange components spatially — side-by-side columns, auto-fit tile grids, and titled cards — while keeping the single-column reading model.

**Use when** two or more components read better beside each other than stacked. Everything collapses to one column on narrow screens and in print, so layout is an enhancement, never a dependency.
