# subsection

_Authoring guidance for the `subsection` component — when to use it, how, and the rules._

A nested section with an `<h3>` heading. Macro: `subsection(id, heading)` — a
`{% call %}` container wrapping child component calls.
