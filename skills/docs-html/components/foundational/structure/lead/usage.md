# lead

_Authoring guidance for the `lead` component — when to use it, how, and the rules._

The opening summary paragraph (larger type). Macro: `lead(text)`.
Use once, as the executive-summary opener.
