# references

_Authoring guidance for the `references` component — when to use it, how, and the rules._

Back-matter numbered citations (auto `[n]`). Macro: `references(items)` where
items is a list of `(id, text)`. Give each an `id` (`ref-<slug>`) so the body can link to it.
