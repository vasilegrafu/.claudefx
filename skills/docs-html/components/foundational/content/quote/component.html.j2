{# purpose: pull quote with attribution #}
{% macro quote(text="", source="") %}
  <blockquote class="pull">
    <p>{{ caller() if caller else text }}</p>
  {% if source %}
    <footer>— {{ source }}</footer>
  {% endif %}
  </blockquote>
{% endmacro %}
