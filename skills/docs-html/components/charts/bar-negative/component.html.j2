{# purpose: a measure that crosses zero — variance, surprise, contribution #}
{# sample: series=[("A",[1,2,3]),("B",[3,2,1])], categories=["x","y","z"], caption="s" #}
{% import "components/charts/_render.html.j2" as r %}

{% macro bar_negative(series=[], categories=[], caption="", height=340, note="", y_name="") %}
{{ r.out(chart_cartesian(series=series, categories=categories, caption=caption,
                          y_name=y_name, kind="bar", tones=True), height, note) }}
{% endmacro %}
